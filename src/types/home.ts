export interface HomeData {
    title: string;
    message: string;
}

export interface ImageHash {
    image: string;
    hash: string;
}

export interface Memory {
    title: string;
    image: string;
    blurHash: string;
    body: string;
}